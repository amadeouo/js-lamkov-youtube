// 1
let name = "name"
console.log(name);

// 2
let age = 25
console.log(`Возраст: ${age}`);

// 3
let a = 20, b = 30
console.log(a + b);
console.log(a - b);
console.log(a * b);
console.log(a / b);

// 4
let isStudent = false
if (isStudent == true) {
    console.log(`Студент: Да`);
} else {
    console.log(`Студент: Нет`);
}


// 5
let height = 1.75
if (height > 1.80) {
    console.log("Является");
} else {
    console.log("Не является");
}

// 6
let x = 10, y = 9
if (x == y) {
    console.log("Числа равны");
} else if (x > y) {
    console.log(`Числа не равны, ${x} Больше чем ${y}`);
} else if (x < y) {
    console.log(`Числа не равны, ${x} Меньше чем ${y}`);
}

// 7
let result = 10 + 20 * 3
console.log(result);

// 8
let score = 20
if (score >= 90) {
    console.log("Отлично");
} else if (70 <= score < 90) {
    console.log("Хорошо");
} else if (score < 70) {
    console.log("Можно лучше");
}

// 9
let status = null
console.log(status == null);

// 10
let c = 1, d = 1
c += d
console.log(c);


/* ✅ Проверка и советы по твоим решениям
1. Переменная name — всё отлично.
2. age — всё правильно, использовал шаблонные строки, молодец!
3. Арифметические операции — всё верно.
4. Проверка isStudent — правильно, хотя можно упростить условие:
if (isStudent) {
  console.log(`Студент: Да`);
} else {
  console.log(`Студент: Нет`);
}
Пояснение: в условии достаточно писать if (isStudent) — это проверяет, что переменная истинна (true).

5. Высота — правильно, только условие чуть более точное:
if (height > 1.80) {
  console.log("Является");
} else {
  console.log("Не является");
}
Может быть полезно уточнить, что именно ты хочешь проверить — например, больше или равно, если нужно.

6. Сравнение x и y — всё правильно.
7. Вычисление — всё отлично.
8. В условии есть ошибка:
else if (70 <= score < 90)
Это не работает в JavaScript так, как ожидаешь, потому что сравнение 70 <= score < 90 — это цепочка, которая интерпретируется как:

(70 <= score) < 90
и результат этого — true или false, превращающийся в число 1 или 0. Поэтому условие всегда истинное или ложное, и не работает как ожидаешь.

Правильное решение:

if (score >= 90) {
  console.log("Отлично");
} else if (score >= 70 && score < 90) {
  console.log("Хорошо");
} else {
  console.log("Можно лучше");
}
Обрати внимание: условие score >= 70 && score < 90 — это правильный способ задать диапазон.

9. Проверка status == null — правильно, но лучше использовать строгое сравнение:
console.log(status === null);
или, чтобы проверить null и undefined:

console.log(status == null); // возвращает true и для null, и для undefined
10. c += d — всё верно.
Ответ на твой вопрос:
Почему в условии 8 выводится "Хорошо", а не "Может лучше"?
Потому что у тебя в условии:

else if (70 <= score < 90)
— это неправильно, как я объяснил выше. В JavaScript это сравнение превращается в:

(70 <= score) < 90
Если score = 20, то:

70 <= 20 — false (которое в числовом виде — 0)
0 < 90 — true
То есть условие всегда воспринимается как true, если score меньше 70, потому что false превращается в 0, и 0 < 90 — всегда true.

Поэтому при score = 20 (или другом значении), у тебя срабатывает ветка "Хорошо", хотя по логике должна — "Можно лучше".

Рекомендуемый код:

if (score >= 90) {
  console.log("Отлично");
} else if (score >= 70 && score < 90) {
  console.log("Хорошо");
} else {
  console.log("Можно лучше");
}
*/