// import { tabsSelectors } from "./tabs.js"

const selectors = {}

export function initModals() {
    console.log('InitModals')
}

// tabsSelectors['идфддвфыдвфд'] = 123 // не сработает потому что файл sliders.js подключен после текущего файла
// console.log('tabsSelectors:', tabsSelectors) // не сработает
// slidersSelector['dasdad'] = 12123 // не сработает
// console.log('tabSelector:', tabsSelectors) // не сработает
